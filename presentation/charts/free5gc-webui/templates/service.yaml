apiVersion: v1
kind: Service
metadata:
  name: {{ include "presentation.fullname" . }}
  labels:
    {{- include "presentation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "presentation.selectorLabels" . | nindent 4 }}
