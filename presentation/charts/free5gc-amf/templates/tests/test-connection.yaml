apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "presentation.fullname" . }}-test-connection"
  labels:
    {{- include "presentation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "presentation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
